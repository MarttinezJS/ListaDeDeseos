import { FiltrolistasPipe } from './filtrolistas.pipe';

describe('FiltrolistasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrolistasPipe();
    expect(pipe).toBeTruthy();
  });
});
